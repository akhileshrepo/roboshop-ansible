- name: Install MySQL client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: load schema
  ansible.builtin.shell: mysql -h 172.31.31.224 -uroot -pRoboShop@1 < /app/schema/{{component}}.sql